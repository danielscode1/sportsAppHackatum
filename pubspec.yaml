name: sports_app_hackatum
description: A Flutter MVP for organizing spontaneous sports events in Munich
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # State management
  flutter_riverpod: ^2.5.1
  hooks_riverpod: ^2.5.1
  flutter_hooks: ^0.20.5

  # Firebase
  firebase_core: ^4.2.1
  firebase_auth: ^6.1.2
  cloud_firestore: ^6.1.0
  firebase_storage: ^13.0.4
  firebase_messaging: ^16.0.4

  # Map
  flutter_map: ^7.0.2
  latlong2: ^0.9.1

  # Location
  geolocator: ^11.0.0
  permission_handler: ^11.3.0

  # Utilities
  intl: ^0.19.0
  image_picker: ^1.0.7

  # UI
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
<<<<<<< HEAD
  uses-material-design: true

  assets:
    - assets/sports/
=======
  uses-material-design: true
>>>>>>> origin/main
